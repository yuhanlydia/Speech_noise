# Speech_noise

Research code for **Selective Acoustic Relevance** in speech/audio language models.

The core hypothesis is that acoustic relevance is relational rather than intrinsic: the same acoustic event or source should be used or ignored depending on the current query.

Development is tracked on feature branches; the approved design and implementation plan live under `docs/superpowers/`.
