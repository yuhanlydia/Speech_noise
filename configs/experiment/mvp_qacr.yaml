model:
  model_id: Qwen/Qwen2.5-Omni-3B
  quantization: nf4
  thinker_only: true
  device_map: auto
  attention_backend: eager
data:
  manifest: data/mvp/pairs.jsonl
  seed: 0
method:
  name: qacr
  layers: [0]
  router_dim: 32
  lambda_switch: 1.0
  lambda_identity: 0.1
  checkpoint: artifacts/qacr/router.pt
output_dir: artifacts/qacr/eval
seed: 0
training:
  epochs: 3
  learning_rate: 0.001
  weight_decay: 0.0
  grad_clip_norm: 1.0
